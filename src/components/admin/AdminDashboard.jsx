import React from 'react'

export default function AdminDashboard(){
  return (
    <div style={{padding:40}}>
      <h1>Admin Dashboard</h1>
      <p>Welcome to the FreightPower Admin panel. This is a placeholder dashboard — you can replace it with the real admin UI.</p>
    </div>
  )
}
